// Example configuration object for a chart
const chartConfig = {
    // Other properties...
    axisYDiscrete: 1,
    // More properties...
};

// In this context, `axisYDiscrete: 1` might configure the Y-axis to treat its scale as discrete.
